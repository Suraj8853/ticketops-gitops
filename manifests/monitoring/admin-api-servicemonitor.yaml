apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: admin-api-monitor
  namespace: ticketops
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: admin-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
