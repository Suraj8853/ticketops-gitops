# These values are applied via helm, not ArgoCD
# Run: helm upgrade prometheus prometheus-community/kube-prometheus-stack -n monitoring -f this-file.yaml
# NOTE: This file is for documentation only - ArgoCD does not apply it
